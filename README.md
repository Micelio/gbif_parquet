# gbif_parquet to rdf/turtle


To convert GBIF occurences into turtle

```
mvn package -Puber
java --add-modules jdk.incubator.vector --enable-native-access=ALL-UNNAMED -jar target/gbif_parquet-0.4.0-SNAPSHOT-jar-with-dependencies.jar --year 2026 --month 01 --aws --output /dev/stdout
```
Although the two options `--enable-native-access=ALL-UNNAMED` and `--add-modules jdk.incubator.vector` are optional.
The option `--aws` downloads the files from AWS OpenData if they are not locally present yet.

There is also an option to build a native executable using graal vm
This requires a locally set up graal vm.

```
mvn package -Pnative
./target/OccurencesToRdf --year 2026 --month 02 --aws --output /dev/stdout
```


# Issues

TODO:

1. Scripts assume local relative paths
2. No streaming from S3 directly yet.
3. Occurences snapshots before 2024/02 are not readable (due to schema changes in the parquet files).

# GBIF parquet --> RDF: ShEx shape graph

4 shapes that represent the RDF generated by  `RowToTurtle.java` from the GBIF parquet files. 

```mermaid
classDiagram
    direction LR

    class OccurrenceShape["OccurrenceShape — gbifocc:(gbifid)"] {
        <<dwc:Occurrence>>
        +gbifterm_gbifID : xsd:string
        +dwc_occurrenceStatus : string ?
        +dwc_basisOfRecord : string ?
        +dwc_individualCount : xsd:integer ?
        +dwc_eventDate : xsd:date ?
        +dwc_day_month_year : gDay/gMonth/gYear ?
        +dwc_decimalLatitude_Longitude : xsd:double ?
        +dwc_coordinateUncertaintyInMeters : xsd:double ?
        +dwc_elevation_depth_accuracy : xsd:double ?
        +wdt_P625 : geo:wktLiteral ?
        +dwc_license : IRI ?
        +dwc_identifiedBy_typeStatus_issue : string star
        +dwc_institutionCode_collectionCode : string ?
        +dwc_publishingOrgKey : gbifpub_IRI ?
    }

    class TaxonShape["TaxonShape — gbifsp:(taxonkey)"] {
        <<dwc:Taxon>>
        +dwc_kingdom_phylum_class : string ?
        +dwc_order_family_genus : string ?
        +dwc_taxonRank : gbifterm_IRI ?
        +dwc_species_infraspecificEpithet : string ?
        +dwc_verbatimScientificNameAuthorship : string ?
    }

    class LocationShape["LocationShape — hashed IRI / nl:(cc)"] {
        <<dwc:Location>>
        +dwc_countryCode : xsd:string
        +dwc_stateProvince : string ?
        +rdfs_label : string ?
        +geo_sfWithin : osmrel_IRI ?
    }

    class GeometryShape["GeometryShape — uncertainty circle"] {
        <<geometry>>
        +geo_asWKT : geo:wktLiteral
    }

    OccurrenceShape --> "0..2" TaxonShape : dwciri toTaxon
    OccurrenceShape --> "0..1" LocationShape : dwciri inDescribedPlace
    OccurrenceShape --> "0..1" GeometryShape : geo hasGeometry
    TaxonShape --> "0..1" TaxonShape : rdfs subClassOf
```
